{"change":"Removed the orphaned desktop app-editing API helpers, fixed the Omi Dev smoke script's bundled-node path, and corrected fork docs and release.sh to name the hosted backends run.sh --yolo uses."}
